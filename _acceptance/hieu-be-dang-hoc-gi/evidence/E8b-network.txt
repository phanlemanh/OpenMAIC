E8b network dump — verifier run (independent), 2026-09-22
dev_server.url = http://localhost:3002 (shared, already-running server reused; not started/stopped by this run)

== FAIL-eligible scope note ==
FAIL-eligible = fetch/XHR to origin http://localhost:3002 (no dev_server.api_base configured, same-origin API).
WebSocket connections (ws://localhost:3002/_next/hmr) are Next.js dev-tooling infrastructure, not app
fetch/XHR traffic — noted, not counted as FAIL-eligible.

== Failed requests (raw, from read_network_requests over the whole tab session) ==
GET http://localhost:3002/api/curriculum-packs  → FAILED: net::ERR_CONNECTION_REFUSED   (during initial page bootstrap)
GET http://localhost:3002/api/curriculum-packs  → FAILED: net::ERR_CONNECTION_REFUSED   (during initial page bootstrap, same burst)
  Immediately followed by a second full "GET /" page load (all assets 200) and, moments later,
  two successful "GET /api/curriculum-packs → 200 OK" calls. Console shows the dev server's HMR
  websocket dropping and reconnecting in the same ~3s window (15:42:03–06Z), i.e. a next-dev
  hot-reload/restart blip at bootstrap of this tab, not a failure caused by, or recurring during,
  any E8b step (learner-profile save, outline generation, or the final preview screen). No
  connection-errors or 5xx occurred after 15:42:06Z; the actual E8b assertion window
  (POST /api/generate/scene-outlines-stream and everything after) is 100% 200 OK.

== 404s seen (ALL within the eval's own explicit allow-list — not counted as failures) ==
GET /api/stages                              → 404  (agent runtime bundle not enabled — by design per eval text)
GET /api/folders                             → 404  (same)
GET /api/persistence/kv/entries/settings-storage        → 404  (server persistence not configured — by design per eval text)
GET /api/persistence/kv/entries/learner-profile-storage → 404  (same)
GET /api/persistence/kv/entries/user-profile-storage    → 404  (same)

== Every other same-origin request in the session ==
All other requests (Next.js static chunks/fonts, /api/server-providers, /api/comfyui-workflows,
/api/access-code/status, /logos/*, /api/curriculum-packs after the blip, and the outline-generation
call POST /api/generate/scene-outlines-stream) returned 200/304. No other 4xx/5xx on this origin.

== Console errors (raw) ==
All console [error] lines are timestamped 2026-09-22T15:42:03.8Z .. 15:42:06.5Z, i.e. the same
bootstrap window as the connection-refused blip above:
  - [KVPersist] Refusing to persist "settings-storage" while its storage is unhydrated...
  - [StageStorage] Failed to list stages: HTTP 404 (x2 — matches the whitelisted /api/stages 404)
  - [Home] Failed to load classrooms / folders: HTTP 404 (matches whitelisted /api/folders 404)
  - [KVPersist] Could not read "user-profile-storage"/"settings-storage"/"learner-profile-storage"
    from the KV account scope: HttpKVStoreError: server persistence not configured (matches the
    whitelisted /api/persistence/kv/entries/* 404s)
  - [KVPersist] Giving up on recovering "<key>" after 3 attempts; storage stays read-only until
    something outside this session changes
  - WebSocket connection to 'ws://localhost:3002/_next/hmr?id=...' failed (x7, dev HMR channel)
  - Failed to load resource: net::ERR_CONNECTION_REFUSED (x2 — the /api/curriculum-packs pair above)
No console errors after 15:42:06Z (i.e none during learner-profile save, outline generation, or the
final ST-neo-dang-doan preview screen).

== networkObserved ==
app-fail
  Reason: two /api/curriculum-packs requests hit net::ERR_CONNECTION_REFUSED, which is
  FAIL-eligible (same-origin fetch) per the strict letter of the network-truth rule — regardless of
  the fact that they were a one-off dev-server hot-reload blip at tab bootstrap, self-resolved
  within the same second, unrelated to the E8b feature, and did not recur during any of the actual
  E8b steps or during the final assertion (all of which are clean 200s). Reported as-is, without
  discretion, per the "connection-error → FAIL even when the frame looks fine" instruction.
