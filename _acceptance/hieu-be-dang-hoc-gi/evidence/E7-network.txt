E7 — network evidence for AC-7 (curriculumAnchor SSE event in the outline stream)
RE-VERIFIED round 4, independent verifier session, 2026-09-22.
Repo HEAD at test time: b5896ba35b0b1f88fcfb90375a7e54d672599639
  "fix(acceptance): cặp dàn ý mù nêu đích danh mô hình, và nói ra lý do khi tuyến trả rỗng"
  (working tree also had the pre-existing uncommitted diff in
  _acceptance/hieu-be-dang-hoc-gi/s4-args.json only — no product code touched by this verify pass).

Dev server: config.yaml dev_server.start, UNMODIFIED and already running (verified via `ps eww`
on the live next-server/next/pnpm process chain — env has PORT=3002,
NEXT_PUBLIC_MAIC_EDITOR_ENABLED=true, NEXT_PUBLIC_PI_CHAT_ENABLED=true,
NEXT_PUBLIC_COURSEWARE_REFERENCE_ENABLED=true, NEXT_PUBLIC_PERSISTENCE=1) on
http://localhost:3002. This verifier did NOT start or stop this server (pre-existing, shared).
Model key present server-side: .env.local has ANTHROPIC_API_KEY (and others) plus an
uncommented DEFAULT_MODEL; server used openai:gpt-5.6 (per the "Building for" composer badge
"OpenAI / GPT-5.6").

Driver: Playwright MCP (mcp__playwright__*), driving the actual on-page flow (profile setup →
homepage composer → Enter Classroom → outline review).

=== Setup ===
Learner profile created via Settings → Learner: name "Bé Minh", Class "lớp 7", Subject "Toán",
Curriculum "Cambridge Lower Secondary", Language "English" — the Learner panel showed
"✓ this curriculum is covered — Cambridge Lower Secondary Mathematics Learner's Book 8" before
Save, confirming a registered pack (skills/agent-runtime/cambridge-lower-secondary-maths-8,
gradesVn: ["lớp 7"], stage 8, unit 12 = "Ratio and proportion" / "Tỉ số và tỉ lệ"). Homepage
composer then showed "Building for: Toán — Cambridge Lower Secondary (English)".

=== Wire-level capture method ===
Before the (re-)generation call, `window.fetch` was monkey-patched in-page (via
mcp__playwright__browser_evaluate) to clone() and accumulate the full response text of any
request whose URL contains "scene-outlines-stream", plus a lightweight scan of every
`"type":"..."` token in that text, in arrival order. Prompt used: requirement box filled with
"tỉ lệ và tỉ số", "Enter Classroom" clicked. This is the SAME live request the browser's own UI
code issues — not a replay or a separate script — so what was captured is exactly the bytes the
app itself received on the wire.

=== Request ===
POST http://localhost:3002/api/generate/scene-outlines-stream -> 200 OK
(two runs in this verifier session, both 200 OK — request #94 or an earlier "Tap to review" pass
used to reach the outline-review screen and take the E7-neo-co-goi.png screenshot, and request
#108, captured with the fetch patch above, used for this wire-level SSE dump.)

Ordered "type" tokens seen in the captured SSE body (types only, in arrival order — includes both
the outer SSE event "type" field and the inner per-outline "type" field, e.g. "slide"/"quiz"):
  languageDirective, courseTitle,
  outline(type=slide), outline(type=slide), outline(type=image), outline(type=slide),
  outline(type=quiz), outline(type=slide), outline(type=slide), outline(type=interactive),
  outline(type=slide), outline(type=quiz), outline(type=slide), outline(type=slide),
  curriculumAnchor,   <-- dedicated top-level SSE event type, appears ONCE, after all 10
                          "outline" events and before "done"
  done

Exact bytes for the curriculumAnchor line (captured verbatim from the cloned Response body,
UTF-8, no transport mojibake this time since it went through browser_evaluate's own JSON
serialization rather than a lossy tool transport):

  data: {"type":"curriculumAnchor","data":"Unit 12 · Ratio and proportion — Cambridge Lower
  Secondary Mathematics Learner's Book 8"}

  data: {"type":"done","outlines":[{"id":"scene_1","type":"slide","title":"Unit Anchor and
  Goals", ...

This is BYTE-IDENTICAL to the anchor text rendered in the DOM at data-state="ST-neo-co-goi"
(confirmed separately via document.querySelector, and visible in evidence/E7-neo-co-goi.png):
  "Unit 12 · Ratio and proportion — Cambridge Lower Secondary Mathematics Learner's Book 8"

=== Server-side derivation check (code, not just wire behavior) ===
app/api/generate/scene-outlines-stream/route.ts computes `curriculumAnchor` ONLY after the
outline stream finishes parsing (`parsedOutlines.length > 0`), via
`deriveCurriculumAnchor({ pack, requirement, outlines: parsedOutlines, language })` imported from
lib/server/curriculum-anchor.ts — a pure function over the already-resolved curriculum pack
(looked up server-side from the learner's subject/curriculum/gradeLabel via findPack) and the
scenes the model already produced. The model's prompt/output never carries a `curriculumAnchor`
key; nothing in buildOutlinePrompt/PROMPT_IDS asks the model for this field. So the event is the
server SUMMARIZING inputs it already has (pack + outlines), not a value round-tripped through the
model. `deriveCurriculumAnchor` places `Unit ${n} · ${title}` before the textbook name and never
emits the word "Stage" (see the function's own doc comment: "KHÔNG chữ «Stage», không mã mục
tiêu").

=== ASSERTION ===
A dedicated SSE event of type "curriculumAnchor" (distinct from the per-scene "outline" events)
IS present in the outline stream, exactly once, server-computed from pack + outlines (model not
asked). Content check: unit name ("Unit 12 · Ratio and proportion") stands FIRST; book name
follows after an em dash; NO framework code and NO word "Stage" opens or appears in the sentence;
the sentence is grammatical, human-readable English (matching the learner's profile language for
this run).
=> PASS.

=== NETWORK TRUTH (scope: dev_server.url http://localhost:3002, no api_base declared) ===
Full non-static request list captured via mcp__playwright__browser_network_requests across this
verifier's whole browser session (profile setup + two outline-generation runs + start of one
scene-content run): 117 same-origin (localhost:3002) requests.
Status breakdown: 200 OK x105 (includes the 2 POST scene-outlines-stream calls, both 200), 304 Not
Modified x11 (conditional GETs for static assets, expected/benign), 1 in-flight
(POST /api/generate/scene-content — deliberately not awaited to completion; this verifier
navigated away once the outline+anchor evidence was captured, to avoid spending an unnecessary
extra LLM generation).
ZERO 4xx, ZERO 5xx, ZERO connection errors/timeouts on any same-origin app-scope request in this
session. No GET /api/stages, /api/folders, or /api/persistence/kv/* were hit at all in this run
(this flow never reaches the agent-runtime or account-persistence code paths), so the "correct by
design" 404 carve-outs in the eval's Expected clause do not apply here — there is nothing to
carve out; the observed set is simply clean.
Third-party/static: none observed outside localhost:3002 and Next.js's own dev asset requests
(fonts, favicon, images) — all 200/304, noted only, not counted as FAIL-eligible.

networkObserved = clean
