Hồ sơ nghiệm thu của repo: contract, evals và evidence cho từng tính năng.
Luật và nghi thức nằm ở skill `acceptance-gate:acceptance` (plugin acceptance-gate-kit).
Mỗi tính năng một thư mục con `_acceptance/<slug>/`; file này và `config.yaml` là phần dùng chung.
